them stash simply copy, working your from - git 
by untracked are that files including - changes local 
all remove want you If upstream. from ones the 
with collide changes staged those if merging prevent still 
will this that however, Note --keep-index. option the add 
then - add git with e.g. - staged already 
you that changes stash to want don't you If 
